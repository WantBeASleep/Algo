#include <iostream>

#include <string>

using namespace std;

int main()
{
    ios::sync_with_stdio(NULL), cin.tie(0), cout.tie(0);

    int N;
    cin >> N;

    for (int i = 0; i != N; ++i) {

    }

    return 0;
}

struct leaf {
    
};

void description(string line) {
    
}